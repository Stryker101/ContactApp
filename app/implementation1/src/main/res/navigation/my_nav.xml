<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/my_nav"
    app:startDestination="@id/contactList">
    <fragment
        android:id="@+id/contactList"
        android:name="com.example.implementation1.ui.ContactListFragment"
        android:label="fragment_contact_list"
        tools:layout="@layout/fragment_contact_list" >
        <action
            android:id="@+id/action_contactList_to_contactCardFragment"
            app:destination="@id/contactCardFragment" />
    </fragment>
    <dialog
        android:id="@+id/contactCardFragment"
        android:name="com.example.implementation1.ui.ContactCardFragment"
        android:label="ContactCardFragment" />
</navigation>